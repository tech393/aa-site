SOFTLY POWERFUL — AUDIO FILES (17 MP3 sessions)

The 17 session MP3s total ~760 MB, so they are NOT inside the shareable .zip.

Where to get them:
- This folder (HANDOFF-FOR-DAN/audio/) in the workspace already contains all 17 files.
- A master copy also lives in /AA-AUDIO-LIBRARY/wp-content/uploads/2014/09/ in the workspace.

What to do:
- Upload all 17 MP3s to your media host / GHL media library / CDN.
- Keep the filenames EXACTLY the same.
- In 2-course.html the player references them as audio/<filename>.mp3 (see the SESSIONS list
  near the bottom of the file). Either upload the audio folder alongside the page, or update
  each f: value to the hosted URL.
